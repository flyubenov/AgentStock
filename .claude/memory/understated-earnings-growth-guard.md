---
name: understated-earnings-growth-guard
description: In-progress brainstorm (session NFLX-NXT-growth-source, PAUSED 2026-08-03) — 5th build_scenarios guard for understated earnings growth (NFLX+NXT); detector APPROVED (gateless corroboration), re-source min(ni,op) awaiting approval
metadata: 
  node_type: memory
  type: project
  originSessionId: 672aae65-44bb-43d7-b563-845af52f5166
  modified: 2026-08-03T21:37:49.744Z
---

STATUS: 2026-08-04 (session **NFLX-NXT-growth-source**) — **DONE + MERGED to `master` (no-ff `de7723c`, LOCAL not pushed).** Feature branch `worktree-understated-earnings-growth-guard` deleted + its worktree removed after merge; **`optimizations` then fast-forwarded to `de7723c` (== master)** so it carries the guard (user asked; was briefly kept at `9de45e2` then advanced). Both branches now at `de7723c`. Merged-master suite green (432). Final whole-branch opus review CLEAN (0 Crit/0 Imp/2 Minor, 1 applied `80e1498` docstring, 1 deferred non-issue). Pre-merge branch commits: `00407c5` spec, `a8760c7` plan, `b2cb83a` predicate, `ce353ba` build_scenarios elif, `80e1498` docstring. Edited harness scripts preserved in this session's scratchpad `understated-harnesses/` (worktree `_scratch/` copies were removed with the worktree). NOTE: master is 12 local commits ahead of `origin/main` — nothing in this line of work is pushed.

**LIVE END-TO-END (REAL engine, not the monkeypatch harness), 2026-08-04:** NFLX GROWTH **$73.93/+0.8%** (was −30%, now ~fair); NXT MID_CAP **$63.35/−30.1%** (was −45%, now modest SELL, correct); KLAC canary **$94.22/−48.4%** (unchanged). Matches the validated design exactly.

**AS-BUILT (SDD, 2 TDD tasks + docs):** `_earnings_understated(fin)` predicate at `engine.py` (after `_earnings_outpaces_revenue`): fires iff eg>0; ni_stmt & op_stmt both present, >0, & >eg; feps>teps. Gateless, NO new constant. Then a 5th `elif` in `build_scenarios` (last before `else`): `raw = min(ni_stmt, op_stmt)`, existing `base=max(0.02,min(raw,cap))` clamp reused. 11 new tests in `test_engine.py` (`_understated_growth_fin` NXT-shaped helper + 6 predicate + 5 scenario/precedence). Docstring @`80e1498` notes the deliberate quarterly-vs-annual exception (2 reviewers paused on it vs `_earnings_non_operating`'s caution — it's the sweep-validated corroboration + upward-only + feps-gated). DEFERRED Minor (non-issue by design): scenario tests assert only `realistic`, not the band — `_band_growth_signal` keys optimistic/pessimistic off `revenue_growth_stmt` (0.203 for NXT) not the re-sourced 0.091, so the bull leg is legitimately wider (revenue is a real upside signal; validated live).

(prior brainstorm state below — design as-shipped matches; kept for provenance:)

**FINAL DESIGN — both live-verified 2026-08-03 (numbers are MEASURED, not estimated):**
- Detector GATELESS statement-corroboration (fires iff eg>0; ni_stmt & op_stmt both present, >0, & >eg; feps>teps). No new constant. `sweep_corroboration.py` over 20 names → fires EXACTLY {NFLX, NXT}. All others fail corrob (KLAC no stmt hist; AMD/AVGO/MU/CRM/CDNS/NVDA/GOOGL eg HIGH; V/MA/ANET/UNH/CSCO/BWXT/MMM ≥1 annual line ≤ eg; META eg<0).
- Re-source `raw = min(ni_stmt, op_stmt)`, then existing `base=max(0.02,min(raw,cap))`. 5th elif before else, after `_earnings_outpaces_revenue`.
- `blast_nflx.py` (edited to final design, run live): NFLX $51.06/−30.4% → **$73.93/+0.8%**; NXT $49.71/−45.2% → **$63.35/−30.1%** (stays SELL, correct). 9 names byte-identical Δ0.00%: KLAC/AMD/V/ANET/NBIS/IREN/CRM/CSCO/BWXT — CRM/CSCO/BWXT unmoved CONFIRMS elif precedence (high-side guards still win). NXT landing is data-drift-sensitive (−23%→−30% across fetch days; verdict robust).
- Harness edits ARE SAVED in worktree `_scratch/understated-earnings-growth/` (blast_nflx.py now: gateless `ni>eg and op>eg`, re-source `min(ni,op)`, tickers +NXT/CRM/CSCO/BWXT; sweep_corroboration.py tickers +NXT & 4 more). UNTRACKED.

(superseded ~90%-locked note:)

**DECIDED THIS SESSION (2026-08-03), both reversing the single-NFLX picks toward something SIMPLER (fewer knobs):**
1. **Detector = GATELESS statement-corroboration — USER APPROVED.** DROP the 0.10 gap entirely (it was never the discriminator: `sweep_corroboration.py` already isolates NFLX 1/30 on `min(ni,op) > eg` alone — the "both annual lines exceed quarterly eg" test does all the work). `_earnings_understated` fires iff: `eg>0`; `ni_stmt` & `op_stmt` both present, `>0`, and `> eg`; `feps>teps`. NO new constant; REMOVES the GROWTH_TRUST_FLOOR reuse. 5th `elif` before `else`, after `_earnings_outpaces_revenue`. NXT & NFLX both reach it (neither trips an earlier guard — NXT: op>0 kills non_operating, feps>teps kills inflated).
2. **Re-source = `raw = min(ni_stmt, op_stmt)` — PRESENTED, AWAITING USER APPROVAL (this is where we paused).** Reverses the earlier forward-implied pick. Serves BOTH (fwd-implied breaks NXT: 0.498→capped→$117 absurd). Symmetric w/ detector (fire quantity == re-source quantity); SELF-LIMITING (correction size == the gap, so no separate over-correction guard needed); conservative (takes lower annual line — NXT op 9.1% not ni 15.1%) & immune to AVGO depressed-trailing inflation. Then existing `base=max(0.02,min(raw,cap))` clamps.

**MEASURED LANDINGS (probes this session, statement FCF):** NFLX `min(ni,op)=0.261`→cap→**~$73.68 (−29%→+2.8%)**; NXT `min(ni,op)=0.091`→**$68.94 (−44%→−23%, stays modest SELL, correct)**. NXT probe = this session's scratchpad `probe_nxt.py` (reconstructs engine-assembled fin incl. real_fcf $513.6M; baseline reproduced $50.09/−44.26% EXACTLY). Re-source choice has ZERO blast radius beyond the firing set (non-firing names byte-identical) — only the gateless detector can move a canary, verified by the sweep step.

**NEXT SESSION — REMAINING STEPS:** (a) get user approval on re-source `min(ni,op)`; (b) present full consolidated design for sign-off; (c) EDIT `blast_nflx.py` (drop 0.10 gap, re-source→min(ni,op), add NXT) + `sweep_corroboration.py` (add NXT + widen list), RE-RUN both live to confirm NFLX~$73/NXT~$69 land & canaries byte-identical; (d) write spec `docs/superpowers/specs/2026-08-03-understated-earnings-growth-design.md` → `writing-plans` → TDD.

**RESUME SEQUENCE (user-specified, 2026-08-02) — one brainstorm, two tickers [decisions above now supersede the OPEN parts]:**
1. Resume THIS single understated-earnings-growth brainstorm with **both NFLX and NXT on the table** (NXT surfaced separately as the same understated-earnings shape but a MORE MODEST case: eg ~2.9% vs annual ~9.1% → gap ~6pp, BELOW the 0.10 gap threshold this design picked for NFLX; so NXT would NOT fire under the current cut).
2. **Re-decide the two open questions against BOTH at once:**
   - **Threshold:** does the detector need to catch NXT's modest 2.9-vs-9.1 case? If yes, the `GROWTH_TRUST_FLOOR (0.10)` gap is too wide — need a smaller gap (or different form). Re-sweep blast radius at the looser threshold (watch KLAC/AMD/V/ANET + all canaries).
   - **Re-source target:** forward-EPS-implied serves ONLY NFLX (lands NFLX ~$73); the **statement line serves BOTH** (targets NFLX ~$73 AND NXT ~$69). Lean toward re-sourcing from the statement line, NOT forward-implied. This REVERSES the earlier user pick of (A) forward-implied — the two-ticker view changes it.
3. Then ONE spec → ONE TDD pass → ONE blast-radius sweep verifying BOTH land (NFLX ~$73, NXT ~$69) + canaries byte-identical.

**Reusable harnesses PRESERVED on disk** (session scratchpad is ephemeral; these were copied into the worktree, UNTRACKED): `_scratch/understated-earnings-growth/` inside the worktree — `sweep_understated.py` (bare forward-implied precondition, RAW yfinance keys earningsGrowth/trailingPE/forwardPE/forwardEps/trailingEps), `sweep_corroboration.py` (statement-corroboration via yahoo.fetch_ev_ebitda_history), `blast_nflx.py` (monkeypatch guard into live engine.run, run with `PYTHONIOENCODING=utf-8`; override earnings_growth→forward-implied trick valid only for names that hit the else-branch), `probe_nflx.py` (reconstructs live NFLX fin). Nothing else changed in the worktree — only .pyc noise; NO engine.py edits, NO spec doc (rejected). For NXT, extend sweep_corroboration.py + blast_nflx.py's ticker lists and re-run.

---
(Prior single-NFLX state below — detector pivot & mechanics still valid; only THRESHOLD and RE-SOURCE are re-opened by the NXT fold-in.)

**Worktree:** `.claude/worktrees/understated-earnings-growth-guard`, branch `worktree-understated-earnings-growth-guard`, base `9de45e2` (== `optimizations`, engine/models byte-identical). Baseline green: **421 tests pass**. Skill flow: `superpowers:brainstorming` (was at step "ask clarifying questions"). Probe script (scratchpad, may be gone): reconstructs live NFLX fin + monkeypatches growth.

**Motivating validation (NFLX, GROWTH tier):** live FV **$50.81 (-29.14%)** vs price $71.71 is TOO BEARISH. Composite reconciles (0.4 dcf $44.16 / 0.4 ev_ebitda $55.99 / 0.2 pe $53.76). Two data traps RULED OUT (both handled correctly): (1) info `fcf_ttm` $25.39B (52% margin, impossible) is overridden live by `real_fcf` → statement $9.46B (engine.py:687 — the `--inputs` harness dump shows the RAW info fcf, NOT what the engine uses; always account for real_fcf); (2) EBITDA-basis [[nflx-ebitda-basis-mismatch]] fix still in place (hist_ebitda_base wired engine.py:579-580,716).

**The gap:** the whole -29% rests on the realistic growth base = info `earnings_growth` **11.1%**, the LOWEST of 5 signals, contradicted by: forward-EPS-implied **20.0%** (fwd $3.82/ttm $3.18; == trailing_pe/forward_pe 22.55/18.78), stmt net-income-growth **26.0%**, stmt op-income-growth **27.9%**, revenue **13.4%**. `build_scenarios`' `else` branch takes `earnings_growth` first unconditionally. The four existing guards (`_earnings_distorted`/`_non_operating`/`_inflated`/`_outpaces_revenue`, engine.py ~158-259) all handle earnings misleadingly HIGH/wrong-signed; NONE handle it misleadingly LOW. This guard is the missing mirror.

**Clean growth sensitivity (statement FCF, live fin):** eg 0.111→FV $50.81 (-29%); 0.134→$55.88 (-22%); 0.1585→$61.90 (-14%); **≥0.20→$73.68 (+2.8%)** [saturates: GROWTH cap ~0.20-0.25 binds]. So re-source target barely matters for NFLX (all ≥ forward land near cap) but matters for blast radius on other names.

**Re-source target CHOSEN by user (2026-08-02):** (A) **forward-EPS-implied rate** (trailing_pe/forward_pe − 1). Keeps detect+re-source on the same forward signal; NFLX→~$73.68.

**HARNESS SCRIPTS (4, saved 2 places — old session scratchpad `…/672aae65-…/scratchpad/` AND kept worktree `.claude/worktrees/understated-earnings-growth-guard/_scratch/understated-earnings-growth/`). Re-read 2026-08-03:**
- `sweep_understated.py` — live-fetches 30 tickers, RAW yfinance keys, tests the DEAD bare-forward-implied precondition (eg>0, feps>teps, implied=tpe/fpe−1 > eg). Fired 12/30. Reference only; detector abandoned.
- `sweep_corroboration.py` — live-fetches 15 eligible/canary names + `fetch_ev_ebitda_history`, tests **pure statement-corroboration min(ni,op) > eg with NO 0.10 gap** (the gap lived only in the code-side REFINED DESIGN, never here). This IS the gateless detector. Verdict-only (no FV). **TO RE-RUN: add NXT + widen ticker list; confirm still isolates to NFLX+NXT.**
- `blast_nflx.py` — THE before→after FV proof harness: monkeypatches `engine.build_scenarios`, runs full live `engine.run()` for NFLX+canaries, prints baseline vs guarded FV+Δ%. **STILL HARDCODES BOTH REVERSED CHOICES: (a) the 0.10 gap `min(ni,op)−eg > m.GROWTH_TRUST_FLOOR` (line 18), (b) re-source `implied = tpe/fpe−1` (line 29). MUST EDIT: drop gap, re-source `raw = min(ni,op)`, add NXT to ticker list (line 41).**
- `probe_nflx.py` — offline NFLX fin reconstruction + growth sensitivity (NFLX analog of this session's `probe_nxt.py`).

**BLAST-RADIUS SWEEP RUN (2026-08-02) — PIVOTS the detection signal.** Fetch uses RAW yfinance keys (earningsGrowth/trailingPE/forwardPE/forwardEps/trailingEps), NOT normalized fin keys. Statement annual growth via `yahoo.fetch_ev_ebitda_history` → net_income_growth/op_income_growth/revenue_growth.
- **Bare forward-EPS-implied (implied=tpe/fpe−1 > eg, feps>teps) is TOO BROAD: fires 12/30**, incl. **KLAC canary (ratio 5.35!)** and cyclical troughs AMD 3.96 / AVGO 2.64 / MU (depressed trailing EPS inflates implied) + ordinary growers V/MA/UNH/CRM/CSCO. NFLX (1.81) sits mid-pack — NO forward-implied threshold isolates it. **The locked forward-implied DETECTOR is dead.**
- **Statement-corroboration ISOLATES NFLX 1/30:** fire iff quarterly info `earnings_growth`>0 AND both `net_income_growth_stmt` & `op_income_growth_stmt` present, positive, and > eg. NFLX: eg 0.111 vs ni 0.261/op 0.279 (2.35×). EVERY other name fails: KLAC has no stmt history (→safe canary); cyclicals' eg is HIGH not low (AMD op .771<eg .912, AVGO op .739<eg .854, MU 6.52<13.7); V/UNH/CSCO/BWXT/MMM/CDNS have ≥1 annual line below eg. This is the [[bwxt-non-operating-growth-source]] like-with-like ANNUAL + both-lines-agree discipline (CEG/DDOG lesson), MIRRORED: `_earnings_non_operating` fires ni>0 & op<=0 (overstated); this fires eg<min(ni,op) both>0 (understated).

**REFINED DESIGN (to present):** 5th elif `_earnings_understated` before else. Detect: eg>0; ni_stmt & op_stmt both present & >0; min(ni,op)−eg > **GROWTH_TRUST_FLOOR (0.10)** [reused, NO new knob; NFLX gap 0.150]; feps>teps [re-source validity + analyst corroboration]. Re-source: raw = trailing_pe/forward_pe−1, then base=max(0.02,min(raw,cap)). **Blast radius EXACTLY 1 (NFLX $50.81/−29% → ~$73.68/+2.8%).** Statement-corroboration ALSO protects the forward-implied re-source from cyclical inflation (a trough can't have both annual lines growing). Residual risk: an AVGO-like one-off trailing DEPRESSION with growing annual lines could over-re-source, but cap ~0.25 bounds it.

**OPEN to confirm with user:** (1) detection pivot forward-implied→statement-corroboration (data forces it); (2) re-source stays forward-implied given detection moved to statements? (alt: re-source from min annual line / revenue_stmt); (3) threshold = gap > GROWTH_TRUST_FLOOR 0.10 (no new constant). Then spec doc → writing-plans → TDD. Relates to [[hood-pe-growth-source-fix]], [[bwxt-non-operating-growth-source]].
