---
name: validating-skill-rr-extension
description: "Active brainstorm (superpowers:brainstorming) — extend the validating-agent-stock skill to ALSO validate the Risk-Reward ratio (the 3rd pipeline). Branch validate-risk-reward @7008ab3. Awaiting user's answer on what R-R validation should focus on."
metadata: 
  node_type: memory
  type: project
  originSessionId: e46cf06c-922b-422f-b20c-ff0d61e596ad
  modified: 2026-08-08T21:04:45.307Z
---

**GOAL:** Extend the `validating-agent-stock` skill so it validates the **Risk-Reward (R-R) ratio** too — the 3rd pipeline shipped by [[risk-reward-rating-engine-design]] (merged master `7008ab3`). The skill today is written for TWO pipelines (Fair Value + Quality) only.

**SESSION SETUP (2026-08-08):** Branch **`validate-risk-reward`** created from master `7008ab3`. In a **superpowers:brainstorming** session (HARD GATE: no code/implementation until a design is presented AND user-approved; terminal step = writing-plans). Prior work (the R-R engine itself) is DONE+MERGED+PUSHED — this is the follow-on skill extension.

**EXPLORED / current state of what we're extending:**
- Skill file: `.claude/skills/validating-agent-stock/SKILL.md` (~18KB) — framed throughout as "a **two-pipeline** Python app" (FV + Quality). Has: codebase map, the validation recipe, the worked-example rule (change MUST carry before→after numbers), the "user asserts too high/low" flow, the "only if a real gap → optimize (TDD)" flow, and the "reuse an existing pattern before inventing" rule.
- Harness: `.claude/skills/validating-agent-stock/validate_ticker.py` — runs `fv_run`+`sc_run` via asyncio.gather and dumps FV breakdown + Quality sections + (`--inputs`) raw extract_financials/cashflow/EV-EBITDA-hist/quarterly-rev. **Does NOT touch risk_reward (predates the 3rd pipeline).**

**MY ANALYSIS of what the extension needs (draft, not yet a design):** (1) reframe two→three pipeline; (2) add R-R to the codebase map (`backend/risk_reward/{config,scoring,engine}.py`, `services/risk_reward_sheets.py`); (3) extend `validate_ticker.py` to also run `risk_reward.engine.run` and dump ratio/tier/reward_score/risk_score/per-slot metric_scores/raw_snapshot/status; (4) an R-R validation recipe (reconcile the ratio: which metrics active, which anchor each hit, reward-axis vs risk-axis weighted avg, coverage floor →N/A, clamp [0.2,5.0], tier map); (5) R-R-specific traps: confidence-scaled analyst weight (`0.08+0.10·c`), coverage floor N/A, the negative-PEG guard just added, **the M5 negative-debtToEquity→safest-leverage-1.0 gap (unfixed, risk-axis mirror of the PEG bug)**, sign artifacts on the risk axis, and that R-R is **config-driven** (anchors/weights in `config.py`) so many "fixes" are anchor re-calibration not deep logic; (6) worked-example rule for R-R = before→after **ratio + axis scores + tier**, measured by re-running `build_metric_scores`+`aggregate` on live inputs with a value swapped; (7) blast-radius note: R-R is ISOLATED from FV/Quality (an R-R change can't move them — a plus), but anchors/weights are GLOBAL so within-R-R blast radius applies across all tickers.

**CONCEPTUAL CRUX to resolve in the design:** unlike FV (judged vs market/DCF) and Quality (vs business fundamentals), **R-R has NO external market truth** — it's a constructed index. So "validate R-R" ≈ internal correctness + tier face-validity + anchor sensibility, NOT "is this the right market number."

**DESIGN AGREED + SPEC COMMITTED `2e2f885`** (`docs/superpowers/specs/2026-08-08-validating-skill-rr-extension-design.md`). Decisions locked via brainstorming:
- **Focus:** internal correctness = PRIORITY; + tier face-validity WITH a grounded qualitative read (growth/risk/moat/issues); + anchor/weight calibration also in scope.
- **Qualitative read:** data-grounded + analyst judgment, every claim labeled DATA vs JUDGMENT, NO new fetches. Moat read from Quality Section II (ROIC/ROIC−WACC) since it's not an R-R metric.
- **Structure:** HYBRID — lean SKILL.md spine (3-pipeline framing, R-R in codebase map, R-R isolated+config-driven, pointer) + NEW `risk-reward-validation.md` reference (reconciliation recipe, qualitative read, traps catalog, ratio worked-example, calibration flow).
- **Trigger:** ON-DEMAND deep R-R (only when the Q is about R-R) + optional ONE-LINE R-R cross-ref on FV/Quality answers (flag contradictions). Harness runs ALL THREE always (R-R core dumped always; `rr_source_inputs` under `--inputs`).
- **Harness:** extend `validate_ticker.py` to add `risk_reward.engine.run` to the gather + a `RISK_REWARD` block (full model_dump: ratio/tier/reward+risk/metric_scores/raw_snapshot/status) + `rr_source_inputs` (info fields incl. analyst-weight inputs) under `--inputs`. Read-only; one extra history fetch.
- **Scope:** docs+harness only, NO engine change. **M5 negative-debtToEquity→safest-leverage gap is DOCUMENTED as a trap, NOT fixed here** (would be a separate validation-driven task).

**SPEC APPROVED by user. PLAN WRITTEN + COMMITTED `0edacfe`** (`docs/superpowers/plans/2026-08-08-validating-skill-rr-extension.md`, 3 tasks): T1 harness `validate_ticker.py` (add rr_run to gather + `RISK_REWARD` model_dump block always + `rr_source_inputs` under --inputs; verify by LIVE harness runs KLAC/IREN/NBIS — no pytest); T2 create `risk-reward-validation.md` (6 sections: intro, metric roster table, reconciliation recipe, qualitative read data-vs-judgment, traps catalog incl. M5 KNOWN/UNFIXED, worked-example+calibration); T3 SKILL.md lean spine edits (description+when-to-use triggers, two→three pipeline, core-principle constructed-index line, 4 R-R codebase-map rows, isolated+config-driven facts, harness note, pointer + on-demand/one-line-cross-ref policy). Plan self-review done (spec §1-§8 all mapped). Verification = live harness runs + content checklists (NO backend change, NO pytest impact).

**IMPLEMENTED via subagent-driven-development (2026-08-08) — ALL 3 TASKS DONE, each reviewed clean; FINAL whole-branch review APPROVE FOR MERGE (0 findings).** Branch `validate-risk-reward` tip `408f0bf`. Commits: harness `9badba2` (RISK_REWARD block + rr_source_inputs, reviewer re-ran live KLAC reconciles), reference `f36b290` (risk-reward-validation.md 6 sections, reviewer verified all 12 anchors vs config.py + §6 arithmetic by hand + M5 gap real), SKILL.md spine `408f0bf` (3-pipeline reframe + map + pointer + on-demand policy, deletion-audit = no FV/Quality collateral damage). Docs+harness only, no engine change, no pytest impact. SDD ledger `.superpowers/sdd/2026-08-08-validating-skill-rr-extension/progress.md`.

**DONE + MERGED + PUSHED (2026-08-09).** Merged to **master no-ff `c8116d7`** and **PUSHED to `origin/main`** (`7008ab3..c8116d7`, clean merge — no interloping commits this time). Per explicit user request the **`validate-risk-reward` branch was KEPT (NOT deleted)**. Backend 481 pass on the merged tree; harness live-verified. SDD workspace left in place (branch kept for possible continued work). Skill registry confirmed the R-R triggers live on master. FEATURE COMPLETE — the `validating-agent-stock` skill now validates all three pipelines (FV, Quality, R-R). NEXT natural follow-on if ever wanted: use the newly-extended skill to actually FIX the documented M5 negative-debtToEquity gap (risk-axis mirror of the negative-PEG bug) — see [[risk-reward-rating-engine-design]] M5 note.
